for loop, execute code a certain amount of time
example:
for(let i = 0;i<=10;i++){
    execute;
}

nested loop:
for(let i=0;i<3;i++){
    console.log('i outer '+i);
    for(let j = 5; j>2; j--){
        sum = sum + j + i;
        console.log('i inner '+i);
        console.log('j inner '+j);
        console.log('sum inner '+sum);
    }
}
console.log(sum);

for-of loop, execute for every element in an array
example:
for(const el of array){
    console.log(el);
}

for-in loop, execute for every key in an object
example:
for(const key in obj){
    console.log(key);
    console.log(obj[key]);
}

while loop, execute as long as condition is true
example:
while(isLoggedIn){
    execute();
}
condition wont change global variable

do while loop, just like while loop but this loop
will execute the code once before checking the condition
example:
do{
    execute();
}while(condition);