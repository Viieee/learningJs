static 
defined with static keyword
only accessible on class itself, not on instance
without instantiation (ex: const class = new Class())
you dont need to use the 'new' method to create an object based on that class\
helper class, global config 
work on the same data


static method in class is used to glue together a bunch of other classes

instance 
you can store different data with the same structure in it